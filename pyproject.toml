[project]
name = "remote-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai[anthropic,openai,google]",
    "fastapi[standard]",
    "markdown",
    "python-dotenv",
]

[project.scripts]
start = "uvicorn server:app --host 0.0.0.0 --port 3000"
